let initialState = {
    friends: [
        {id:1, name: "Anna"},
        {id:1, name: "Tom"},
        {id:1, name: "Jack"}
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;